type: array
items:
  type: object
  properties:
    attachmentContent:
      type: string
      example: "base64 encoded attachment"
    attachmentName:
      type: string
      example: "attachment"
    attachmentExtension:
      type: string
      example: "txt"
  required:
    - attachmentContent
    - attachmentName
    - attachmentExtension
