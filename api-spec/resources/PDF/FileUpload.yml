post:
  description: Upload a static file to DB for later use
  summary: Upload a static file
  operationId: uploadFile
  tags:
    - PDF
  requestBody:
    required: true
    content:
      multipart/form-data:
        schema: 
          $ref: '../../schemas/_index.yml#/FileSaveRequest'
  responses:
    '200':
      description: successful operation
    '400':
      description: Invalid name or data supplied

  # This API has no security
  security: []
