post:
  description: Save a static PDF to DB for later use
  summary: Save a static PDF
  operationId: savePDF
  tags:
    - PDF
  requestBody:
    required: true
    content:
      application/json:
        schema: 
          $ref: '../../schemas/_index.yml#/PDFSaveRequest'
  responses:
    '200':
      description: successful operation
    '400':
      description: Invalid name or data supplied

  # This API has no security
  security: []
