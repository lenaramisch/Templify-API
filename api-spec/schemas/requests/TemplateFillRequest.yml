type: object
properties:
  shouldBeSent:
    type: boolean
    example: true
  toEmail:
    type: string
    example: "holmes.sherlock@example.com"
  toName:
    type: string
    example: "Sherlock Holmes"
  subject:
    type: string
    example: "TestSubject"
  placeholders:
    type: object
    properties:
      FirstName:
        type: string
        example: "Sherlock"
      LastName:
        type: string
        example: "Holmes"
      Age:
        type: string
        example: "45"
    required:
      - FirstName
      - LastName
      - Age
required:
  - shouldBeSent
  - toEmail
  - toName
  - subject
  - placeholders
